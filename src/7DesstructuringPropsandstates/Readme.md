first way:destructure functional component it self in the  in functional parameter ({name})
second way (props)=>{
    const {name,age}=props
}

class destructure 
after render()
const{properties here}